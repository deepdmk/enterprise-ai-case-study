# Agno Framework (must be 2.4.0+)
agno>=2.4.0

# Agno dependencies (required for proper functionality)
openai>=1.0.0

# Testing dependencies
pytest>=9.0.0
pytest-asyncio>=0.23.0

# HTTP client for tests
httpx>=0.27.0

# YAML parsing for config tests
pyyaml>=6.0

# TOML parsing for pyproject tests (Python 3.10 compat)
tomli>=2.0.0
